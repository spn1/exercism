// use clock::exercism::Clock;

fn main() {
    // // let left = Clock::new(1, 20);
    // // let new = left.add_minutes(-600);

    // // println!("{}", new);
    // println!("🚩 -10 / -5: {}", -10 / -5);
    // println!("🚩 -10 / 5: {}", -10 / 5);
    // println!("🚩 10 / -5: {}", 10 / -5);

    // let clock = Clock::new(2, -60);
    // println!("{}", clock)
}